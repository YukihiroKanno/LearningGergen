<h1 class="my_page_title"><%= @usr+"さんのページ" %></h1>
<br><br><br>
<section class = "blog">
<h2 class="blog-headline">会員リスト</h2><br>    
<h3 class="menu_heda">過去問</h3>
  <br>
  <% if @answers.present? %>
    <% @answers.each do |f| %>
     <h5 class="title_h3">平成<%= f.year %>  事例<%= f.jirei %>　<%= f.created_at.strftime('%Y/%m/%d') %></h5>      
    <% end %>
   <br>
　<% else %>
      <p>登録された過去問はありません。</p>
      <br>
  <% end %>
  
<h3 class="menu_heda">事例問題</h3>
  <br>
  <% if @drills.present? %>
    <table border="1">
    <tr>
      <th>試験年度</th><th>事例</th><th>順位</th><th>得点</th><th>コメント数</th>
    </tr>  
    <% @drills.each do |f| %>
     <tr>
       <td></td><td></td><td></td><td></td><td></td>
     </tr>
    <% end %>
   </table>
   <br>     
  <% else %>
      <p>登録された事例企業分析はありません。</p>
      <br>    
  <% end %>

<h3 class="menu_heda">Tolk</h3>
  <br>
  <% if @titles.present? %>
    <% @titles.each do |f| %>
       <h5 class="title_h3"><%= link_to f.title , title_communications_path(f.id) %>  <%= link_to f.created_at.strftime('%Y/%m/%d'),title_communications_path(f.id) %>　</h5>           
    <% end %>
  <% else %>
      <p>Tolkに設定したテーマはありません。</p>
      <br>
  <% end %>
 <br>
 <br>
<h3 class="menu_heda">プロフィール</h3>
  <br>
  <% if @prof.present? %>
    <% @prof.each do |f| %>
       <h5 class="title_h3"><%= f.created_at.strftime('%Y/%m/%d') %><%= button_to "削除", destroy_mine_path(f.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>　</h5>           
    <% end %>
  <% else %>
      <p>プロフィールは、登録されていません。</p>
      <br>
  <% end %>  
<br>
<br>
<br>
 <%= link_to :back do %>
   <button>戻る</button>
 <% end %>
</section>
   <div class = "sidebar">
     <section class="popular">
        <h2 class="sidebar-headline">サイトメニュー</h2>
        <div class="sidebar-contents">
          <p class="popular-text">　<%= link_to '会員リスト', members_path %></p>      
          <p class="popular-text">　<%= link_to 'Tolk', titles_path %></p> 
          <p class="popular-text">　<%= link_to '過去問分析', answers_path %></p>
          <p class="popular-text">　<%= link_to '事例企業分析', drills_path %></p> 
         </div>
      </section>
   </div>
 <br>
 <br>
 </div>
